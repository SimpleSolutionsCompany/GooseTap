import 'package:flutter/material.dart';

class AirdropScreen extends StatelessWidget {
  const AirdropScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("Airdrop Screen")));
  }
}
